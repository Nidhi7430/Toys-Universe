version: "3.4"
services:
  client:
    image: node:16-alpine
    ports:
      - "3000:3000"
    working_dir: /app/client
    volumes:
      - ./client:/app/client
    env_file:
      - ./client/.env
    command: sh -c "npm install && npm start"
volumes:
  po-data:
